{
  "name": "cafe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build-extension": "npm run build && cp assets/. dist/ -a"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.28.4",
    "jsdom": "^21.1.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.1",
    "vitest": "^0.28.4"
  }
}
