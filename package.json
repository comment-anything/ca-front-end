{
  "name": "cafe",
  "private": true,
  "version": "0.0.5",
  "scripts": {
    "dev": "vite",
    "dev-p": "vite --mode production",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build-extension": "npm run build && cp assets/. dist/ -a",
    "build-extension-w": "npm run build && Xcopy /E /I assets dist",
    "dist": "vite build --mode production && cp assets/. dist/ -a",
    "dist-w": "vite build --mode production && Xcopy /E /I assets dist"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.28.4",
    "jsdom": "^21.1.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.1",
    "vitest": "^0.28.4"
  },
  "browser": "window"
}
